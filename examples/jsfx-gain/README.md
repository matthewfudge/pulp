# Pulp JSFX Gain

Minimal source-only gain example for Pulp's bounded JSFX subset.

Validate it with:

```bash
python3 tools/scripts/jsfx_subset.py doctor \
  --file examples/jsfx-gain/PulpJsfxGain.jsfx
```
