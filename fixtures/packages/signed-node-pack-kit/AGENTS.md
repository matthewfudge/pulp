# Signed Node Pack Kit

This fixture models metadata for a signed `pulp_node_v1` pack. Validation must verify manifest paths and platform claims only; loading the dynamic library belongs to the host node-pack validation lane after trust review.
