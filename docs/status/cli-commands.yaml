# Pulp CLI command status
# Vocabulary: stable, usable, experimental, partial, planned, unsupported

commands:
  - name: build
    status: usable
    summary: Configure and build the project. Auto-detects CMake reconfiguration needs.
    args:
      - name: extra_args
        kind: passthrough
        description: Passed through to cmake --build (e.g., --target, -j)
    docs: reference/cli.md#build

  - name: test
    status: usable
    summary: Run the test suite via CTest. Builds first if needed.
    args:
      - name: extra_args
        kind: passthrough
        description: Passed through to ctest (e.g., -R for filtering)
    docs: reference/cli.md#test

  - name: status
    status: usable
    summary: Show project root, git info, build state, source counts, SDK availability.
    docs: reference/cli.md#status

  - name: validate
    status: usable
    summary: Run plugin format validators (clap-validator, auval) on built plugins.
    docs: reference/cli.md#validate

  - name: ship
    status: experimental
    summary: Signing and packaging subcommands (sign, package, check). macOS only.
    subcommands:
      - name: sign
        summary: Code-sign all built plugin bundles
        args:
          - name: --identity
            required: true
          - name: --entitlements
            required: false
      - name: package
        summary: Create .pkg installers
        args:
          - name: --version
            required: false
      - name: check
        summary: Check signing status of built plugins
    docs: reference/cli.md#ship

  - name: docs
    status: usable
    summary: Browse local documentation and status manifests.
    subcommands:
      - name: index
        summary: List available docs
      - name: search
        summary: Search docs for a string
      - name: open
        summary: Print a doc by slug
      - name: show support
        summary: Look up support status
      - name: show command
        summary: Look up a CLI command
      - name: show cmake
        summary: Look up a CMake function
      - name: show style
        summary: Show code style rules
      - name: check
        summary: Validate docs consistency against codebase
      - name: build-site
        summary: Generate static docs site
      - name: build-api
        summary: Generate API reference from public headers (Doxygen)
    docs: reference/cli.md#docs

  - name: new
    status: usable
    summary: Create a new plugin project from templates. Checks environment, scaffolds, builds, and tests. (Alias: create)
    args:
      - name: name
        kind: positional
        required: true
        description: Plugin name (e.g., "My Gain")
      - name: --type
        kind: option
        description: "Plugin type: effect, instrument, app, bare (default: effect)"
      - name: --manufacturer
        kind: option
        description: Manufacturer name (default: Pulp)
      - name: --output
        kind: option
        description: Output directory (default: examples/<name>/)
      - name: --no-build
        kind: flag
        description: Skip build after scaffolding
      - name: --no-interactive
        kind: flag
        description: Non-interactive mode for CI/scripting (alias: --ci)
    docs: reference/cli.md#new

  - name: run
    status: usable
    summary: Launch a standalone Pulp application from the build directory.
    args:
      - name: target
        kind: positional
        required: false
        description: Target name (default: first standalone binary found)
    docs: reference/cli.md#run

  - name: upgrade
    status: usable
    summary: Update the Pulp CLI binary to the latest or a specific version.
    args:
      - name: version
        kind: positional
        required: false
        description: Version to install (default: latest)
    docs: reference/cli.md#upgrade

  - name: doctor
    status: usable
    summary: Diagnose environment issues. Checks compiler, CMake, git-lfs, external SDKs, platform deps.
    args:
      - name: --fix
        kind: flag
        description: Auto-fix issues where possible (with confirmation)
      - name: --ci
        kind: flag
        description: Non-interactive mode, exit codes only
      - name: --dry-run
        kind: flag
        description: Show what --fix would do without doing it
    docs: reference/cli.md#doctor

  - name: clean
    status: usable
    summary: Remove the build directory.
    docs: reference/cli.md#clean

  - name: help
    status: usable
    summary: Print usage information.
    docs: reference/cli.md#help
