# Pulp's pinned Shipyard release.
#
# Shipyard is the cross-platform CI controller Pulp uses for queue,
# multi-platform validation, and merge gating. Pulp pins a specific
# Shipyard release here so every checkout uses the same version.
#
# To install or upgrade:
#   ./tools/install-shipyard.sh
#
# To check what's installed vs what's pinned:
#   ./tools/install-shipyard.sh --status
#
# To bump the pin (after verifying a new Shipyard release works on
# Pulp's CI matrix), use:
#   shipyard pin bump --to vX.Y.Z
#
# That flow owns the pin edit and worktree-safety checks; keep the
# lockstep doc skim below as part of the bump PR.
#
# Lockstep docs:
# When bumping the pin, also re-check that the following docs still
# accurately describe what Shipyard provides at the new version:
#   - README.md (Contributing + Security & CI policy section)
#   - CONTRIBUTING.md (Contributor expectations section)
#   - .agents/skills/ci/SKILL.md (tool-selection logic)
#   - docs/guides/local-ci.md (cross-references between local_ci.py
#     and shipyard, eventual removal once local_ci.py is retired)
# If a Shipyard release introduces a new command, deprecates one, or
# changes governance defaults, update those docs in the same PR that
# bumps this pin.

[shipyard]
# v0.22.9 — nine patch releases on the v0.22.x line:
#
#   v0.22.1 — `install.sh` grew `SHIPYARD_VERSION` and
#             `SHIPYARD_INSTALL_DIR` env-var support so downstreams
#             (like Pulp's `tools/install-shipyard.sh`) can delegate
#             the download + verify path without reimplementing it.
#   v0.22.2 — completed the daemon-spawn fix that v0.22.1's commit
#             message advertised but didn't actually include.
#   v0.22.3 — removed "Automated by Shipyard." branding and "Ship"
#             internal-term leakage from auto-opened PR bodies so
#             Pulp PRs read as first-party.
#   v0.22.4 — PyInstaller bundling fix: the standalone binary was
#             missing the `encodings.idna` codec, so `shipyard daemon`
#             crashed silently the moment it tried to resolve a
#             hostname (common in the live-mode webhook listener).
#             0.22.4 adds the codec to the frozen bundle.
#   v0.22.5 — `shipyard daemon status` IPC read-loop fix: the client
#             stopped reading after the hello frame and never consumed
#             the status reply, so `status` reported "not running"
#             even when the daemon was alive. 0.22.5 also adds a CI
#             smoke test for the PyInstaller bundle so future
#             frozen-binary-only regressions (like 0.22.4's idna
#             codec omission) are caught pre-release.
#   v0.22.6 — `shipyard ship-state reconcile`: heals stale ship-state
#             entries by re-polling dispatched GitHub Actions runs
#             and mirroring their terminal status into the cached
#             shipyard queue. Closes the "PR merged upstream but
#             shipyard still thinks it's running" failure mode.
#   v0.22.7 — reconcile evidence-snapshot fix: 0.22.6 healed the
#             `dispatched_runs` layer but left `evidence_snapshot`
#             stale. The macOS GUI's ShipStatePoller applies
#             evidence_snapshot AFTER dispatched_runs and overwrote
#             the rendered status — so reconcile ran but PR cards
#             kept the old "failed" pill. 0.22.7 mirrors terminal
#             statuses into evidence_snapshot so both layers heal
#             together.
#   v0.22.8 — periodic 30s reconcile loop inside the daemon: every
#             active ship-state is re-fetched against GitHub's
#             `statusCheckRollup` and healed if drift is detected.
#             Permanent fix for the "stale failed pill after a
#             re-run" class of bugs. 0.22.6 added a manual command
#             + startup-only auto-reconcile (closed the daemon-
#             outage gap but not re-runs); 0.22.7 taught reconcile
#             to heal the evidence_snapshot layer the GUI actually
#             reads; 0.22.8 runs that reconcile continuously so
#             drift from GitHub-side re-runs self-heals within 30s.
#             Cost: ~120 REST calls/hr per active PR — marginal vs
#             the 5000/hr budget even at 10+ tracked PRs.
#   v0.22.9 — auto-opened PR titles + bodies use the HEAD commit's
#             subject and body instead of "Ship <branch>" + empty.
#             Previous versions produced unhelpful boilerplate; PRs
#             shipped via `shipyard pr` now read as first-party and
#             reviewers see the commit's own rationale without
#             clicking through to the diff.
#   v0.23.0 — daemon wait primitive (Shipyard #150) so long-poll
#             callers (like the macOS GUI) can block on ship-state
#             changes instead of polling. Unused from pulp-side
#             currently; it's the building block v0.25.0's daemon
#             IPC shortcut relies on.
#   v0.24.0 — `shipyard pr` walks past the mechanical "chore: bump
#             versions" commit when composing the PR title and body
#             (Shipyard #151). Previously the auto-PR text pointed
#             at the bump commit (generic, uninformative) instead
#             of the feature commit that actually explained the
#             change. Pulp PR #624 was the canonical repro: title
#             read "chore: bump versions" and the body was
#             "Automated by `shipyard pr`." — the tool-branding
#             text was also scrubbed. v0.24.0 resolves both.
#   v0.25.0 — two semantic changes pulp benefits from directly:
#               * `Version-Bump: <surface>=<level>` trailer is now
#                 authoritative (Shipyard #152). Previously, if an
#                 author wrote `=patch` but the heuristic classified
#                 the diff as minor, the heuristic silently won.
#                 v0.25.0 uses the trailer exactly as written (with
#                 a `reason` string as the justification-of-record).
#                 Pulp's `tools/scripts/version_bump_check.py` is
#                 synced with this behaviour in the same PR that
#                 bumps the pin.
#               * `shipyard ship-state list` is served from the
#                 running daemon via IPC when available, bypassing
#                 the ~5-6s PyInstaller cold-start on every call
#                 (Shipyard #154). The macOS GUI polls this every
#                 7s, so the CPU-time saving compounds. Pulp's
#                 `pulp pr` also hits this path during skill-sync
#                 and version-bump preflight.
#   v0.26.0 — daemon subscribe-replay now uses blocking `put()`
#             instead of `put_nowait()`, so a long-running daemon no
#             longer stops accepting fresh IPC subscribers once the
#             replay ring grows past 64 events. This is the load-
#             bearing fix for the macOS GUI dropping into "polling"
#             mode with no active PR cards after an hour-plus of
#             daemon uptime. Also bumps the IPC protocol to 2,
#             advertises the daemon's own `shipyard_version` in
#             hello/status frames, and teaches `shipyard doctor`
#             to flag daemon-vs-CLI version drift.
#   v0.27.0 — daemon tunnel supervisor: a Tailscale Funnel transient
#             (the funnel process drops momentarily, common on
#             flaky networks) no longer kills the daemon. The
#             supervisor restarts the funnel on backoff and keeps
#             webhook delivery alive. Also adds a periodic
#             reconcile loop that closes the daemon-vs-GitHub
#             state-drift gap independently of the per-PR poll.
#   v0.27.1 — `shipyard ship` preflights the SSH targets BEFORE
#             `gh pr create` runs. Earlier the PR was opened first
#             and ship aborted on unreachable SSH backend, leaving
#             stranded PRs with no validation. Now an unreachable
#             target fails fast and the PR is never opened.
#             Directly addresses the pulp-side "PR opened but ship
#             never finished" pattern that left #627 / #629 / #638
#             stuck mid-flight on Apr 22.
#   v0.27.2 — worktree-local `.shipyard.local/` falls back to the
#             primary checkout's `.shipyard.local/` when the
#             worktree's copy is missing. Pulp uses worktrees
#             heavily for parallel agent work; without this every
#             new worktree had to manually `cp ../primary/.shipyard.local/config.toml`
#             before `shipyard pr` could see the SSH host config.
#             v0.27.2 inherits it automatically.
#   v0.28.0 — `shipyard run` / `shipyard pr` now print the
#             underlying backend error (bundle upload failure,
#             remote `cmake` apply failure, SSH transport error,
#             ssh-keygen mismatch, etc.) below the summary table
#             instead of the bare `ubuntu     error     ssh    12s`
#             row. Lands the diagnosis blind spot called out by
#             pulp #665 (no signal from a 17-second SSH error).
#             Symbol-level fix: error rows now carry the captured
#             stderr tail in the rendered output.
#   v0.29.0 — macOS binary is Developer-ID-signed and Apple-
#             notarized. macOS 26.3+ XProtect skips deep scan for
#             notarized binaries — cold-start on `shipyard pr`
#             drops ~4-5x (from ~5-6s to ~1-1.5s). Also adds a
#             heartbeat status line during long validation phases
#             so a 20-minute lane shows progress instead of a dead
#             terminal, and the bundled Python is bumped to 3.13
#             with lazy-imported rich for further startup wins.
#   v0.30.0 / v0.31.0 / v0.32.0 / v0.33.0 — internal iteration on
#             the release-workflow + CLIXML decode path that
#             ultimately shipped clean in v0.34.0/v0.35.0. Those
#             intermediate tags either had broken release.yml
#             (pre-PEP-668-fix) or were partial CLIXML-decode
#             drops. Pulp never pinned them; the jump is v0.29.0
#             → v0.35.0 directly.
#   v0.34.0 — PEP 668 fix in the tagged release.yml so the
#             auto-release pipeline can install CLI deps on
#             externally-managed Python environments (Namespace
#             runners especially). Also finishes `--skip-target`
#             semantics and SSH preflight messaging.
#   v0.35.0 — ssh-windows bundle-apply transport: raw stderr now
#             lands at `<log>.bundle-apply-stderr` *before* the
#             CLIXML decoder runs, so when the decoder falls back
#             (malformed / truncated envelopes) we have bytes on
#             disk to analyse instead of gibberish on stderr.
#             Lands Pulp-side issue Shipyard #200 (I filed 4/23
#             after #188/#189 turned out to only cover run-step
#             stderr, not the bundle-apply transport path).
#   v0.36.0 — `install.sh` preserves the Developer-ID notarization
#             on the extracted binary (v0.29.0 signed the binary
#             but the installer path was stripping xattrs, so
#             every fresh install went through Gatekeeper deep-scan
#             again). Restores the ~4-5x cold-start win across
#             installs, not just first-run.
#   v0.37.0 — stuck-queued watch annotation: when a GitHub Actions
#             check stays `queued` past N minutes, shipyard now
#             annotates the summary row with "(stuck queued Xm —
#             infra issue?)" instead of just showing blank status
#             forever. Would've named the Namespace Windows
#             queue-stuck case on 4/23 in one glance instead of
#             after multiple `gh run view` loops.
#   v0.38.0 — ssh-windows UTF-8 prelude: shipyard prepends
#             `chcp.com 65001 >nul 2>&1 &` to every command
#             dispatched through the ssh-windows backend so
#             non-ASCII argv (em-dashes in test names, etc.)
#             survives the remote host's default OEM code page.
#             Lands Pulp-side issue Shipyard #208 (I filed 4/23
#             after 46 ctest filter-no-match failures traced
#             back to Namespace Windows image's CP-1252 default).
#             With this in place, Pulp's own build.yml `chcp 65001`
#             workaround (PR #697) can be reverted — shipyard now
#             owns the fix for every consumer.
#   v0.39.0 — ssh-windows bundle-apply path fix: upload + apply
#             paths now agree on how to resolve the remote bundle
#             destination. Pre-fix, uploads of `C:/Users/<user>/
#             shipyard.bundle` ended up at a different on-disk
#             location than what `apply` tried to open, surfacing
#             as `error: could not open 'C:/Users/.../shipyard.bundle'`.
#             Lands Pulp-side issue Shipyard #210 (I filed 4/24
#             with forensics from Shipyard #201's <log>.bundle-
#             apply-stderr artifact).
#   v0.40.0 — extends the #210 fix to cover two additional path
#             shapes Codex P1 (Shipyard #213) flagged: `/`-prefixed
#             paths like `/tmp/shipyard.bundle` (POSIX-shaped
#             configs) and UNC paths (`\\server\share\...`). With
#             this pin, the upload-side `is_rooted` predicate in
#             `bundle/git_bundle.py` matches the apply-side
#             `_is_windows_absolute_path` in `executor/ssh_windows.py`
#             for every shape — no more split-brain between the two
#             layers regardless of `remote_bundle_path` config.
#             After this pin, Pulp `shipyard pr` should validate
#             Windows cleanly without admin-merge.
#
# Cumulative additions from earlier releases still in effect:
#   v0.4–v0.8 — release-bot helpers, `shipyard doctor release-chain`,
#   trailer shortcuts, `watch-stream`, `ship --auto-merge`, cloud
#   retarget (Shipyard PRs #45/#46/#51/#52/#54/#55/#56/#57/#59/#62/#64/#66).
#   v0.3.0 — `shipyard ship --resume` / `--no-resume`, `ship-state
#   list/show/discard`, `cleanup --ship-state`.
#   v0.2.0 — incremental bundles, `--resume-from` SSH, targets/config,
#   cloud polling timeout, ssh upload, stale queue recovery.
#
# Lands at `~/.local/bin/shipyard` via the upstream installer delegate
# in `tools/install-shipyard.sh` (v0.22.1+ `SHIPYARD_VERSION` support).
# The older `~/.pulp/bin/shipyard` path is cleaned up on install to
# prevent shadow-on-PATH bugs.
version = "v0.46.0"

# Public release source. The bootstrap script downloads from
# https://github.com/{repo}/releases/download/{version}/{asset}
repo = "danielraffel/Shipyard"

# SHA-256 verification asset name (downloaded alongside the binary).
checksums_asset = "checksums.sha256"

# Per-platform binary asset names. The bootstrap script picks the
# right one for the current host.
[shipyard.assets]
darwin-arm64  = "shipyard-macos-arm64"
darwin-x64    = "shipyard-macos-x64"
linux-arm64   = "shipyard-linux-arm64"
linux-x64     = "shipyard-linux-x64"
windows-x64   = "shipyard-windows-x64.exe"
