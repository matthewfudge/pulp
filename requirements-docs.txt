# Docs build dependencies — MkDocs Material parallel-build lane (#577 PR 1).
# Pinned to minor versions for CI reproducibility; bump via the standard
# dependency-update workflow in CLAUDE.md.
mkdocs-material>=9.5,<10
mkdocs-awesome-pages-plugin>=2.9,<3
mkdocs-git-revision-date-localized-plugin>=1.2,<2
pymdown-extensions>=10.7,<11
