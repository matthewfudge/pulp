# Pulp JSFX Delay

Source-only memory-based delay example for Pulp's bounded JSFX subset.

Validate it with:

```bash
python3 tools/scripts/jsfx_subset.py doctor \
  --file examples/jsfx-delay/PulpJsfxDelay.jsfx
```
